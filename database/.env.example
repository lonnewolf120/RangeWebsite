# Local (non-Docker) development only. Copy to database/.env.
# See backend/.env.example for the matching local Postgres setup.

DATABASE_URL=postgresql://test:test@localhost:5434/rangewebsite_test

# Must match backend/.env.example — both packages seed/read the same
# bootstrap admin account.
ADMIN_BOOTSTRAP_EMAIL=admin@cyberrange.test
ADMIN_BOOTSTRAP_PASSWORD=ChangeMe123!
